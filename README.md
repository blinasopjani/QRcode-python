# QR Code Generator

A simple Python app to create QR codes from any URL. Generate, preview, and save QR codes easily using a clean graphical interface.

## Features

- Generate QR codes from URLs  
- Preview QR code in the app  
- Save QR code as PNG


```bash
pip install qrcode[pil] pillow 
